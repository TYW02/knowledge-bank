
# Compare match event occurs
> Output pin changes state because the counter value has reached the value stored in the Capture Compare Register

## PWM Register
> The Period Register or (Auto-Reload Register) defines the frequency by determining when the counter rolls over.

## Output Frequency
$f_{PWM} = \frac{f_{timer}}{(PSC + 1)(ARR + 1)}$
