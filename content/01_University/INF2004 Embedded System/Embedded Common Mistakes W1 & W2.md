
# Writing to GPIO Output
> [!GPIO Output]-
> What happens when a `1` (HIGH) is written to a push-pull GPIO output ?
> [[GPIO Output]]

> [!GPIO output]-
> What happens when a `0` (LOW) is written to a push-pull GPIO output ?
> [[GPIO Output]]

> [!Register]
> In the context of a GPIO hardware inside an MCU, which register is responsible for determining whether a pin acts as an input or an output ?
> 
> [[GPIO Output#Which Register is responsible for whether a pin acts as input or output ?|Register]]

> [!Handshake]
> In a GPIO handshaking interface, what is the purpose of the 'ACK' (Acknowledge) signal sent by the receiver to the sender ?
> 
> [[GPIO Output#Handshake|Handshake]]

## Masking
> [!Masking]
> Calculate the mask required to read only a bit 3 of a register using a bitwise AND operation in C.
> 
> [[GPIO Output#Masking|Masking]]


# Interrupt
> [!Masked Interrupt]
> What happens when an interrupt is 'masked' ?
> > [!Answer]-
> > [[Masked Interrupt]]

> [!Edge vs Level]
> What is the primary difference between an edge-triggered interrupt and a level-triggered interrupt.
> 
> [[Edge VS Level Interrupt#Edge vs Level Interrupt|Edge VS Level Interrupt]]
## Timer / Counter Interrupt
> [!Causes]
> What are the specific causes for a Timer/Counter interrupt family ?
> >[!Answer]-
> >[[Timer Counter Interrupt]]

## Watchdog timer interrupt
> [!Purpose]
> What is the purpose of a Watchdog timer interrupt ?
> > [!Answer]-
> > [[Timer Counter Interrupt#Watchdog Purpose|Watchdog Purpose]]

# Ohms
> [!Scenario]
> You need to drive a red LED ($V_f = 2.0V$) from a $3.3V$ GPIO pin and want to limit the current to approximately $1.3mA$.
> > [!Answer]-
> > [[Ohms' Law]]











