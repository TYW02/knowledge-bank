
> Algorithm begins modifying more significant bits (Beyond the first right-most bit), leading to visible distortions.

- This happens because the capacity of the LSBs has been **exhausted**, **forcing** the data into bits that **impact image quality more heavily**.
