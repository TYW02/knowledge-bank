
> An attacker finds 2 **different** inputs that produce the **exact same** hash output.

- This happens because the output space of the hash function is **finite** while the input space is **infinite**.













